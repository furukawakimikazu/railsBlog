<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>RailsBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
        <h1 class="text-center"><%= link_to "RailsPosts", root_path %></h1>
      <nav>
          <% if user_signed_in? %>
            <div class="text-right login-success">
              <%= link_to 'プロフィール変更', edit_user_registration_path, class: 'btn btn-success' %>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
            </div>
          <% else %>
            <div class="text-right login-unsuccess">
              <%= link_to 'サインアップ', new_user_registration_path, class: 'btn btn-success' %>
              <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-success' %>
            </div>
          <% end %>
      </nav>
  </header>

  <%= render 'layouts/messages' %>
    
    <%= yield %>
  </body>
</html>
